{
  "name": "@garden-siege/combat",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic lane battle simulation — shared by NestJS replay worker (Unity client uses C# port)",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"no lint configured\""
  },
  "dependencies": {
    "@garden-siege/protocol": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
